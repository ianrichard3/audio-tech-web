services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vue-web
    ports:
      - "8080:3000"      # expone 8080 del VPS -> 3000 dentro del contenedor
    restart: unless-stopped
    environment:
      - PORT=3000